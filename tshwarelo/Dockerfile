# Use an official PHP image with Apache
FROM php:8.1-apache

# Copy all files from the current folder (tshwarelo) into the web server's root
COPY . /var/www/html/

# Expose port 80 (standard HTTP port)
EXPOSE 80